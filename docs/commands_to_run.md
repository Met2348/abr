# Commands To Run

This file is auto-generated by `scripts/experiment_command_logger.py`.

Purpose:
1. Keep a normalized catalog of command families.
2. Avoid copy-paste drift when rerunning old experiment recipes.

How to populate:

```bash
bash scripts/run_with_exp_log.sh \
  python -u scripts/phase_a_generate_and_eval.py --help
```

After you run through the logger wrapper, this file will be rebuilt from:
- `assets/artifacts/command_logs/run_records.jsonl`.
