# Command Results

This file is auto-generated by `scripts/experiment_command_logger.py`.

Purpose:
1. Keep compact per-run outcomes.
2. Preserve key metrics only (drop progress-bar noise).
3. Track repeated runs via `family_run_index`.

How to populate:

```bash
bash scripts/run_with_exp_log.sh \
  python -u scripts/phase_b_eval.py --help
```

Raw complete logs are kept under:
- `assets/artifacts/command_logs/raw_logs/`.
